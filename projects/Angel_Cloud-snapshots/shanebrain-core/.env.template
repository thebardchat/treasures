# =============================================================================
# ShaneBrain Core - Environment Variables Template
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp .env.template .env
# 2. Fill in your actual values
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. Run: chmod 600 .env (restricts permissions)
# =============================================================================

# =============================================================================
# BASE PATHS (Required - Update for your system)
# =============================================================================

# Path to your 8TB external drive mount point
SHANEBRAIN_ROOT=/mnt/8TB/ShaneBrain-Core

# Data directories (auto-created by setup script)
WEAVIATE_DATA_PATH=${SHANEBRAIN_ROOT}/weaviate-config/data
MONGODB_DATA_PATH=${SHANEBRAIN_ROOT}/mongodb-data
LLAMA_MODELS_PATH=${SHANEBRAIN_ROOT}/llama-configs/models
BACKUPS_PATH=${SHANEBRAIN_ROOT}/backups

# =============================================================================
# MONGODB CONFIGURATION
# =============================================================================

# Local MongoDB (default - works offline)
MONGODB_LOCAL=true
MONGODB_LOCAL_PORT=27017
MONGODB_LOCAL_HOST=localhost
MONGODB_LOCAL_DATABASE=shanebrain_db

# MongoDB Atlas (optional cloud backup)
# Leave empty if not using Atlas
MONGODB_ATLAS_ENABLED=false
MONGODB_ATLAS_URI=
# Example: mongodb+srv://shanebrain_db_user:YOUR_PASSWORD@thebardchat.qo97nbq.mongodb.net/?retryWrites=true&w=majority&appName=thebardchat

# Database name (same for both local and Atlas)
MONGODB_DATABASE=shanebrain_db

# =============================================================================
# WEAVIATE CONFIGURATION
# =============================================================================

# Local Weaviate (default - works offline)
WEAVIATE_LOCAL=true
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_GRPC_PORT=50051

# Weaviate Cloud (optional backup)
# Leave empty if not using cloud backup
WEAVIATE_CLOUD_ENABLED=false
WEAVIATE_CLOUD_URL=
WEAVIATE_CLOUD_API_KEY=

# Embedding configuration
WEAVIATE_VECTORIZER=text2vec-transformers
# Alternative: text2vec-ollama (if using Ollama)

# =============================================================================
# LLAMA MODEL CONFIGURATION
# =============================================================================

# Default model to use
LLAMA_DEFAULT_MODEL=llama-3.2-3b
# Options: llama-3.2-3b, llama-3.2-11b, llama-3.2-1b

# Model paths (relative to LLAMA_MODELS_PATH)
LLAMA_3B_MODEL=llama-3.2-3b-instruct-q4_K_M.gguf
LLAMA_11B_MODEL=llama-3.2-11b-vision-instruct-q4_K_M.gguf

# Inference settings
LLAMA_CONTEXT_LENGTH=4096
LLAMA_MAX_TOKENS=2048
LLAMA_TEMPERATURE=0.7
LLAMA_GPU_LAYERS=0
# Set GPU_LAYERS > 0 if you have GPU acceleration

# Ollama settings (if using Ollama instead of llama.cpp)
OLLAMA_ENABLED=false
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2

# =============================================================================
# LANGCHAIN CONFIGURATION
# =============================================================================

# Tracing (optional - for debugging)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=shanebrain-local

# Memory settings
LANGCHAIN_MEMORY_KEY=chat_history
LANGCHAIN_MAX_MEMORY_MESSAGES=50

# =============================================================================
# PLANNING SYSTEM CONFIGURATION
# =============================================================================

# Planning file locations
PLANNING_ROOT=${SHANEBRAIN_ROOT}/planning-system
PLANNING_ACTIVE=${PLANNING_ROOT}/active-projects
PLANNING_COMPLETED=${PLANNING_ROOT}/completed-projects
PLANNING_TEMPLATES=${PLANNING_ROOT}/templates

# Auto-save settings
PLANNING_AUTOSAVE=true
PLANNING_AUTOSAVE_INTERVAL=60
# Interval in seconds

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Encryption key for sensitive data (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=

# API rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Audit logging
AUDIT_LOG_ENABLED=true
AUDIT_LOG_PATH=${SHANEBRAIN_ROOT}/logs/audit.log

# =============================================================================
# PROJECT-SPECIFIC SETTINGS
# =============================================================================

# Angel Cloud
ANGEL_CLOUD_CRISIS_THRESHOLD=0.7
ANGEL_CLOUD_EMERGENCY_CONTACT=
ANGEL_CLOUD_LOCAL_RESOURCES_PATH=${SHANEBRAIN_ROOT}/angel-cloud/resources

# Pulsar AI
PULSAR_SECURITY_SCAN_INTERVAL=300
PULSAR_THREAT_DB_PATH=${SHANEBRAIN_ROOT}/pulsar/threat-db

# LogiBot
LOGIBOT_DATABASE=${MONGODB_DATABASE}
LOGIBOT_DISPATCH_INTEGRATION=false

# ShaneBrain Legacy
LEGACY_VOICE_ENABLED=false
LEGACY_FAMILY_ACCESS_ENABLED=false
LEGACY_PRESERVATION_MODE=local

# =============================================================================
# DOCKER SETTINGS
# =============================================================================

# Docker compose project name
COMPOSE_PROJECT_NAME=shanebrain

# Container resource limits
DOCKER_MEMORY_LIMIT=4g
DOCKER_CPU_LIMIT=2

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Debug mode (disable in production!)
DEBUG=false
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Hot reload for development
HOT_RELOAD=false

# =============================================================================
# BACKUP SETTINGS
# =============================================================================

# Automatic backups
AUTO_BACKUP_ENABLED=true
AUTO_BACKUP_INTERVAL=86400
# Interval in seconds (86400 = daily)

# Backup retention
BACKUP_RETENTION_DAYS=30
BACKUP_COMPRESSION=true

# Cloud backup (requires WEAVIATE_CLOUD or MONGODB_ATLAS)
CLOUD_BACKUP_ENABLED=false

# =============================================================================
# END OF TEMPLATE
# =============================================================================
# After filling in values:
# 1. Save as .env
# 2. Run: chmod 600 .env
# 3. Run: python scripts/setup_credentials.py --verify
# =============================================================================
